(package contrib.match
  
  (root "src")
  (products (match (test match.validate.run)
                   (update match
                           match.validate.run)
                   (dependencies jazz.dialect)))
  (project ".project"))
