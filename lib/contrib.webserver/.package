(package contrib.webserver
  
  (root "src")
  (products (webserver (alias contrib.webserver))
            (contrib.webserver (update webserver
                                       webserver.implementation.http)
                                 (dependencies jazz.dialect
                                               gambit.dialect)))
  (project ".project"))
