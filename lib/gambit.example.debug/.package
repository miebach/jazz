(package gambit.example.debug
  
  (root "src")
  (products (gambit-debug (unit gambit.example.debug.product)
                          (update gambit.example.debug.product)
                          (dependencies jazz.dialect
                                        scheme.dialect)))
  (project ".project"))
