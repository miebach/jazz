(package gambit
  
  (root "src")
  (products (gambit.dialect (update gambit)
                            (dependencies jazz.dialect
                                          scheme.dialect)))
  (project ".project"))
