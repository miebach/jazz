(package jazz.benchmark
  
  (root "src")
  (products (benchmark (run jazz.benchmark.run)
                       (update jazz.benchmark)
                       (dependencies jazz.dialect)))
  (project ".project"))
