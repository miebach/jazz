(package profiler
  
  (root "src")
  (products (profiler (update profiler)
                      (dependencies jazz.dialect
                                    scheme.dialect)))
  (project ".project"))
