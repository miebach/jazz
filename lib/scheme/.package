(package scheme
  
  (root "src")
  (products (scheme.dialect (update scheme
                                    scheme.syntax-rules)
                            (dependencies core)))
  (project ".project"))
